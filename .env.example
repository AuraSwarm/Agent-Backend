# Deprecated: configuration is now in config/app.yaml (see config/app.yaml.example).
# This file is kept only for reference; use config/app.yaml for server, database, and dashscope_api_key.

# Required for DashScope (Qwen) chat and embedding
DASHSCOPE_API_KEY=sk-xxx

# Optional: OpenAI fallback for embedding
# OPENAI_API_KEY=sk-xxx

# Database (default matches docker-compose)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/agent_backend

# Redis (Celery broker)
REDIS_URL=redis://localhost:6379/0

# MinIO (archives)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=archives

# Server (for agent-backend serve and ./run)
# HOST=0.0.0.0
# PORT=8000

# Config directory (relative to project root)
# CONFIG_DIR=config

# Comma-separated list of required env vars checked at startup (optional override)
# REQUIRED_ENV_VARS=DASHSCOPE_API_KEY
